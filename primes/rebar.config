
{erl_opts, [debug_info]}.

{deps, [espace]}.

{dialyzer, [ {plt_extra_mods, [espace]} ]}.

%% {plugins, [ rebar3_hex ]}.

%% {profiles,
%%  [ {dev, [ {edoc_opts, [{private, true}]} ]},

%%    {chunks, [ {edoc_opts, [{doclet, edoc_doclet_chunks},
%%                            {layout, edoc_layout_chunks},
%%                            {preprocess, true}
%%                           ]} ]}
%%  ]}.

%% {ex_doc,
%%  [ {homepage_url, "https://fredyouhanaie.github.io/XXX"},
%%    {source_url, "https://github.com/fredyouhanaie/XXX"},
%%    {output, "exdoc"},
%%    {api_reference, true}
%%  ]}.
